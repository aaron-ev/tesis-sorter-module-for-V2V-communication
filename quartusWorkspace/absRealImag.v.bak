

module absRealImag#(parameter QI = 5, parameter QF = 11)
(
	input [QI-1:-QF] inReal;
	input [QI-1:-QF] inImag; 
	output outAbsReal,
	output outAbsImag
);

assign outAbsReal = (inReal[QI-1] == 1) ? 1:0;
assign outAbsImag = (inImag[QI-1] == 1) ? 1:0; 

endmodule
